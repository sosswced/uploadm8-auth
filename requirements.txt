# UploadM8 Backend Dependencies
# Compatible with Python 3.11 on Render
# Install with: pip install -r requirements.txt

# Web Framework - use version ranges for pre-built wheels
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic[email]>=2.6.0,<3.0.0

# Database
asyncpg>=0.29.0,<1.0.0

# Redis
redis>=5.0.0,<6.0.0

# AWS S3/R2
boto3>=1.34.0,<2.0.0

# Authentication
PyJWT>=2.8.0,<3.0.0
bcrypt>=4.1.0,<5.0.0
cryptography>=42.0.0,<44.0.0

# HTTP Client
httpx>=0.26.0,<1.0.0

# Stripe Billing
stripe>=8.0.0,<12.0.0

# Email validation for pydantic
email-validator>=2.1.0,<3.0.0

# Multipart form data
python-multipart>=0.0.6,<1.0.0

# Video Processing (Worker)
# Note: FFmpeg must be installed separately
# Add to Render build command: apt-get update && apt-get install -y ffmpeg
